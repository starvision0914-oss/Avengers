---
name: osan-blog-expansion-status
description: 오산 홈페이지 208개 블로그 글 1500자 보강 작업 — 2026-07-13 전체 완료
metadata: 
  node_type: memory
  type: project
  originSessionId: 22f4c8ff-2f78-4400-9e88-27bf11b821dd
---

**2026-07-13 완료**: 208개 글 전부 1500자 이상 달성(`check_len.php` 재조회 결과 미달 0건). ID 294~204 범위(90개)를 메인 세션에서 직접 순차 처리로 마무리. API 키(ANTHROPIC_API_KEY) 없이 각 글의 기존 톤/구조를 파악한 뒤 Claude(나 자신)가 직접 새 H2 섹션 2~3개를 작성해 삽입하는 방식.

과거 기록(2026-07-11 기준 63개 완료, 145개 미달)은 이제 stale — 아래는 그 시점의 교훈이며 여전히 유효.

**진행상태 확인법**: `wp eval-file`로 `get_posts()` 순회하며 `mb_strlen(wp_strip_all_tags(strip_shortcodes($post->post_content)))` < 1500인 ID 목록 조회. 미완료 ID는 129,131-160(일부),163-293 범위에 다수 분포(정확한 최신 목록은 재조회 필요, 매번 바뀜).

**재개 시 방법**: `<!-- wp:heading {"level":3} -->\n<h3>관련 글</h3>` 마커 앞에 PHP str_replace로 H2+문단 블록 삽입 → `wp_update_post()`.

**교훈 1 - 문단 길이 계산 착각**: 4-5문장 한글 문단을 "250~300자"로 예상했지만 실측은 150~200자 수준. 매번 1500자 미달로 2~3라운드 보강이 반복됨. 다음엔 처음부터 목표보다 훨씬 크게(3~4개 섹션, 각 300자+ 체감) 써야 함. (07-13 재확인: 이번에도 2섹션 첫 시도 후 대부분 60~600자씩 부족해 2~3차 top-up이 필요했음 — 이 과소평가 패턴은 거의 항상 재발하므로, 처음부터 넉넉히 쓰고도 top-up 라운드를 기본으로 예정해두는 게 낫다.)

**교훈 3 - API 키 없이도 가능**: content_gen.py의 ANTHROPIC_API_KEY(.env)가 비어있어 외부 API 호출 스크립트는 무용지물이었음. 실제로는 매번 Claude가 세션 내에서 직접 글을 써서 PHP 삽입 스크립트(문자열 치환+wp_update_post)로 넣는 방식 — 앞으로도 이 프로젝트의 "AI로 글쓰기" 작업은 API 키 확인 없이 바로 직접 작성 방식으로 접근할 것.

**교훈 2 - 병렬 서브에이전트가 세션 한도 소진**: 182개 글을 3개 백그라운드 에이전트(각 60개)로 병렬 처리 시도 → 각 에이전트가 컨텍스트를 새로 구성하며 개별 글 내용을 다시 읽어 3배 중복 소모 → 세션 토큰 한도 도달로 3개 모두 실패(2026-07-11, 4:50am KST 리셋). 이후 메인 세션에서 직접 순차 처리로 전환. **향후 대량 작업은 서브에이전트 병렬화보다 메인 세션에서 직접 작은 배치(10~20개)로 순차 처리하는 게 안전.**

관련: [[project_osan_homepage]]
