---
name: project_11st_jinag7460_name_opt
description: "11번가 jinag7460(등급1위 계정) 15,395건 상품명 최적화 진행 상태 — 트리밍 적용 중, 키워드 추가 2단계 대기"
metadata: 
  node_type: memory
  type: project
  originSessionId: 65854cbc-8aa2-4568-95c3-193c08d99126
---

## 배경
- 사용자가 "1등급계정 먼저 진행해"로 지정. `ElevenSellerGrade` 최신 스냅샷(2026-06-04) 기준 grade=1은 jinag7460(스타블루) 유일.
- `optimize_11st_product_names.py` (2026-07-01 작성, 미커밋)에 두 가지 버그 있었음: ①`ANTHROPIC_API_KEY` 미설정(.env 빈값) ②`status_type='103'` 필터가 실제 DB값(한글 '판매중')과 안 맞아 0건 매칭 → 둘 다 발견 후 후자만 코드 수정, 전자는 API 키 대신 세션 내(Claude Code) 직접 판단으로 우회.

## 분석 결과 (15,395건, status_type='판매중')
- 삭제예정 2건: 7717849004(모의총포/서바이벌총), 9433008380(나이키 브랜드 무단추정)
- 확인필요 3건(사용자 최종판단 대기): 9438057130·9456589210·9459194873 — 전부 "무좀방지" 계열 양말, 업계 통용표현이라 위험도 낮음
- 트리밍 변경 5,871건: 대부분 50byte 초과(5,844건, 최대 117byte까지 있었음 — 11번가는 구등록 상품에 50byte 제한을 강제하지 않는 것으로 실측 확인됨) 이름 중복단어 제거+뒷부분 트림, 나머지는 1+1/이벤트/사은품 등 클린체크 위반 문구 제거
- 유지 9,519건: 문제없음

## 검증 방법 (API 키 없이 진행한 방식)
- 캐릭터/브랜드 위험후보(151건)는 키워드 스캔 후, 애매한 건은 hulk API의 `detailContent`(상세페이지, 보통 이미지 1장으로 구성)를 다운로드해 Read 툴로 직접 시각 확인. 짱구퍼즐=학산문화사, 마블퍼즐=퍼즐라이프, 뽀로로캔디=정식 라이선스 확인됨(제조사·상품코드 명시). 이 판단 기준으로 나머지 유사패턴(뽀로로/핑크퐁/타요 등 특정 제품라인+제조사 있는 것)은 정상 처리, "정글도"(원예용 낫 vs 도검) 등 애매한 건은 이름에서 위험단어만 제거.
- **중요 발견**: `ElevenMyProduct.product_name`(DB)과 실제 hulk API `productName`이 다른 경우가 1건 있었음(8438362107) — 무작위 6건 재검증은 전부 일치해 예외케이스로 판단, 전반적으로 DB는 신뢰 가능.

## 적용(라이브 반영) 진행 상황
- 스크립트: `/tmp/claude-.../scratchpad/jg_apply.py` (진행상황 `/tmp/jg_apply_progress.json`, 로그 `/tmp/jg_apply.log`)
- `guard.preflight(wait=True, wait_timeout=3600)`로 11st 락 대기 후 자동 시작 — 광고전략/광고비 등 다른 예약작업과 순번대기 발생, 몇 시간 대기하기도 함.
- 최초 --sleep 1.5(초당 처리 2.65초) → 사용자 요청으로 --sleep 0.6(초당 처리 약 1초)로 재시작, 진행상황은 progress 파일로 이어받아 재개.
- 2026-07-01 21:14 기준 2,257/5,866건 완료, 에러 5건(개별 400 응답, 패턴성 없음).

## 2단계: 카테고리 기반 키워드 추가 (준비 완료, 트리밍 배치 끝나면 적용 예정)
- 방법: 엑셀상품관리 다운로드(`/tmp/eleven_excel_jinag7460/`)의 카테고리 경로(예 "문구/사무용품>보드/칠판>코르크 게시판")에서 리프+상위 카테고리 단어를 후보로, 50byte 여백 있을 때만 추가.
- **셀러 카테고리 오분류 발견**: 예) "일본정품 켄트 칫솔"이 "공구>작업/수공구>기타 수공구"로 분류돼있어 그대로면 이상한 키워드가 붙음. 이를 막기 위해 리프 카테고리별 공통어휘(같은 카테고리 다른 상품명들의 공유 토큰)와 겹치는지 체크해서 안 겹치면 스킵하는 필터 적용.
- 결과: 4,431건 후보 → 필터 후 3,341건 확정(2,693건은 애매/오분류 의심으로 보수적 제외).

**Why:** API 키 없이도 15K 규모 상품명 정비를 안전하게 처리하는 방법이 확립됨 — 향후 다른 계정에도 같은 방식 적용 가능.
**How to apply:** 다음 세션에서 이어서 할 때 `/tmp/jg_apply.log`·`/tmp/jg_apply_progress.json`으로 진행상황 확인. 트리밍 배치 완료 확인 후 키워드 추가(jg_final_kw2.json) 단계 실행. 확인필요 3건은 사용자 최종 결정 필요.
